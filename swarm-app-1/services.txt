- networks:
    docker network create --driver overlay frontend
    docker network create --driver overlay backend

- volumes:
    docker volume create postgres

- services:
    docker service create --name vote --network frontend --publish 80:80/tcp --replicas 2 dockersamples/examplevotingapp_vote:before
    docker service create --name redis --network frontend --replicas 1 redis:3.2
    docker service create --name worker --network backend frontend --replicas 1 dockersamples/examplevotingapp_worker
    docker service create --name db --network backend --mount postgres:/var/lib/postgresql/data --replicas 1 postgres:9.4
    docker service create --name result --network backend --publish 5001:80 --replicas 1 dockersamples/examplevotingapp_result:before

